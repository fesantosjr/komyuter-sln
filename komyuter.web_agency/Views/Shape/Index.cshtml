@model IEnumerable<komyuter.core.DomainClasses.Shapes>

@{
    ViewBag.Title = "Shape List";
    Layout = "~/Views/Shared/_LayoutSpark.cshtml";
}


<div class="wrapper">

    @Html.Partial("_SparkSideBarPartial")

    <div class="main">

        @Html.Partial("_SparkNavBarPartial")

        <main class="content">
            <div class="container-fluid">

                <div class="header">
                    <h1 class="header-title">
                        Shape
                    </h1>
                    <nav aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item active" aria-current="page">Listing</li>
                        </ol>
                    </nav>
                </div>
                <div class="row">
                    <div class="col-12">
                        <div class="card">
                            <div class="card-header">
                                <button id="createnew" class="btn btn-pill btn-primary" value="Shape/Create">Create New Shape</button>
                            </div>
                            <div class="card-body">
                                <table id="datatables-basic" class="table table-striped" style="width:100%">
                                    <thead>
                                        <tr>
                                            <th>@Html.DisplayNameFor(model => model.id)</th>
                                            <th>@Html.DisplayNameFor(model => model.shape_id)</th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.shape_pt_lat)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.shape_pt_lon)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.shape_pt_sequence)
                                            </th>
                                            <th>Delete</th>
                                        </tr>
                                    </thead>
                                    <tbody>

                                        @foreach (var item in Model)
                                        {
                                        <tr>
                                            <td>
                                                <a href="@Url.Action("Edit", new { id = item.id })" title="Details">@Html.Raw(item.id)</a>
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.shape_id)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.shape_pt_lat)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.shape_pt_lon)
                                            </td>
                                            <td>
                                                @Html.DisplayFor(modelItem => item.shape_pt_sequence)
                                            </td>
                                            <td>
                                                <a href="@Url.Action("Delete", new { id = item.id })" title="Delete"><i class="align-middle fas fa-fw fa-trash"></i></a>
                                            </td>
                                        </tr>
                                        }

                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </main>

        @Html.Partial("_SparkFooterPartial")

    </div>

</div>